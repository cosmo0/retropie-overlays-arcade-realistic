#include "overlays/common/{{orientation}}.cfg"

input_overlay = overlays/{{game}}.cfg

custom_viewport_width = "{{width}}"
custom_viewport_height = "{{height}}"
custom_viewport_x = "{{x}}"
custom_viewport_y = "{{y}}"